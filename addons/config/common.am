
pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = $(PACKAGE_NAME).pc

EXTRA_DIST = README.txt MATLAB

MAINTAINERCLEANFILES = \
        aclocal.m4 \
        config.h \
        config.h.in \
        configure \
        Makefile.in \
	stamp-*
